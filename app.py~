import bottle;

request = bottle.request;
response = bottle.response;
app = bottle.Bottle();
render = bottle.template;

@app.get('/hello/<name>')
def say_hello(name):
    return render('Hello, {{name}}!', name=name);

app.run(host='localhost', port=8080, debug=True, reloader=True);
